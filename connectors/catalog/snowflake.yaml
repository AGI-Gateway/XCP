# Generated by scripts/seed-catalog.py — edit there, or add your own file.
# Nothing here is secret. Credentials are vault:// REFERENCES resolved at runtime.
id: snowflake
name: Snowflake
vendor: Snowflake Inc.
description: Snowflake via its Model Context Protocol endpoint.

endpoint:
  url: https://mcp.snowflake.com/mcp
  transport: streamable_http
  mcp_spec: "2026-07-28"

verification:
  status: unconfirmed
  source: https://mcpplaygroundonline.com/blog/awesome-mcp-servers
  checked: "2026-09-08"

auth:
  method: oauth2_auth_code
  scopes: []
  secrets:
    client_id: vault://env/connectors/snowflake#client_id
    client_secret: vault://env/connectors/snowflake#client_secret

trust:
  class: unknown
  min_tier: A1xH2
  min_write_tier: A2xH2

scopes_exposed:
  - mcp:tools/snowflake.query
  - mcp:tools/snowflake.list_tables

tags: [data, warehouse]
maintainer: anonymous
notes: >
  Endpoint is per-account and warehouse-managed; substitute your account
  URL.
