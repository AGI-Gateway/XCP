# Generated by scripts/seed-catalog.py — edit there, or add your own file.
# Nothing here is secret. Credentials are vault:// REFERENCES resolved at runtime.
id: google-workspace
name: Google Workspace
vendor: Google LLC
description: Google Workspace via its Model Context Protocol endpoint.

endpoint:
  url: https://<your-operator-host>/google-workspace/mcp
  transport: streamable_http
  mcp_spec: "2026-07-28"

verification:
  status: self_hosted
  source: ""
  checked: "2026-09-08"

auth:
  method: oauth2_auth_code
  scopes: [https://www.googleapis.com/auth/drive.readonly]
  secrets:
    client_id: vault://env/connectors/google-workspace#client_id
    client_secret: vault://env/connectors/google-workspace#client_secret

trust:
  class: unknown
  min_tier: A0xH1
  min_write_tier: A1xH2

scopes_exposed:
  - mcp:tools/gworkspace.drive_search
  - mcp:tools/gworkspace.calendar_create

tags: [productivity, storage, email]
maintainer: anonymous
notes: >
  No single vendor-hosted MCP endpoint for Workspace as a whole. Domain-wide
  delegation grants access to every user in the tenant — prefer per-user
  consent and readonly scopes.
