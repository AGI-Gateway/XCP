# Generated by scripts/seed-catalog.py — edit there, or add your own file.
# Nothing here is secret. Credentials are vault:// REFERENCES resolved at runtime.
id: salesforce
name: Salesforce
vendor: Salesforce, Inc.
description: Salesforce via its Model Context Protocol endpoint.

endpoint:
  url: https://mcp.salesforce.com/mcp
  transport: streamable_http
  mcp_spec: "2026-07-28"

verification:
  status: unconfirmed
  source: https://www.usecarly.com/blog/chatgpt-mcp-servers/
  checked: "2026-09-08"

auth:
  method: oauth2_auth_code
  scopes: [api, refresh_token]
  secrets:
    client_id: vault://env/connectors/salesforce#client_id
    client_secret: vault://env/connectors/salesforce#client_secret

trust:
  class: unknown
  min_tier: A1xH2
  min_write_tier: A2xH2

scopes_exposed:
  - mcp:tools/salesforce.soql
  - mcp:tools/salesforce.update_record

tags: [crm, enterprise]
maintainer: anonymous
notes: >
  If you use the JWT bearer flow instead, the signing key is high-value —
  managed secret backend only, never the env backend.
