# Generated by scripts/seed-catalog.py — edit there, or add your own file.
# Nothing here is secret. Credentials are vault:// REFERENCES resolved at runtime.
id: square
name: Square
vendor: Block, Inc.
description: Square via its Model Context Protocol endpoint.

endpoint:
  url: https://mcp.squareup.com/mcp
  transport: streamable_http
  mcp_spec: "2026-07-28"

verification:
  status: unconfirmed
  source: https://www.usecarly.com/blog/chatgpt-mcp-servers/
  checked: "2026-09-08"

auth:
  method: oauth2_auth_code
  scopes: []
  secrets:
    client_id: vault://env/connectors/square#client_id
    client_secret: vault://env/connectors/square#client_secret

trust:
  class: unknown
  min_tier: A2xH2
  min_write_tier: A2xH2

scopes_exposed:
  - mcp:tools/square.list_payments
  - mcp:tools/square.create_refund

tags: [payments, financial]
maintainer: anonymous
notes: >
  Money-moving: A2xH2 for every call.
