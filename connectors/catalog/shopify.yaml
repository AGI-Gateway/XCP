# Generated by scripts/seed-catalog.py — edit there, or add your own file.
# Nothing here is secret. Credentials are vault:// REFERENCES resolved at runtime.
id: shopify
name: Shopify
vendor: Shopify Inc.
description: Shopify via its Model Context Protocol endpoint.

endpoint:
  url: https://mcp.shopify.com/mcp
  transport: streamable_http
  mcp_spec: "2026-07-28"

verification:
  status: unconfirmed
  source: https://www.usecarly.com/blog/chatgpt-mcp-servers/
  checked: "2026-09-08"

auth:
  method: oauth2_auth_code
  scopes: []
  secrets:
    client_id: vault://env/connectors/shopify#client_id
    client_secret: vault://env/connectors/shopify#client_secret

trust:
  class: unknown
  min_tier: A1xH1
  min_write_tier: A2xH2

scopes_exposed:
  - mcp:tools/shopify.search_products
  - mcp:tools/shopify.create_order

tags: [ecommerce, financial]
maintainer: anonymous
notes: >
  Order and refund tools move money — treat writes as financial.
