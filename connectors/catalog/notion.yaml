# Generated by scripts/seed-catalog.py — edit there, or add your own file.
# Nothing here is secret. Credentials are vault:// REFERENCES resolved at runtime.
id: notion
name: Notion
vendor: Notion Labs
description: Notion via its Model Context Protocol endpoint.

endpoint:
  url: https://mcp.notion.com/mcp
  transport: streamable_http
  mcp_spec: "2026-07-28"

verification:
  status: confirmed
  source: https://docs.stacklok.com/toolhive/guides-mcp/notion-remote
  checked: "2026-09-08"

auth:
  method: oauth2_auth_code
  scopes: []
  secrets:
    client_id: vault://env/connectors/notion#client_id
    client_secret: vault://env/connectors/notion#client_secret

trust:
  class: probed
  min_tier: A0xH1
  min_write_tier: A1xH1

scopes_exposed:
  - mcp:tools/notion.search
  - mcp:tools/notion.fetch
  - mcp:tools/notion.create_pages
  - mcp:tools/notion.update_page

tags: [productivity, knowledge]
maintainer: anonymous
notes: >
  Official remote server. OAuth 2.1. Point at mcp.notion.com rather than a
  third-party wrapper of the Notion API.
